<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">{% assign dposts = page.deta %}{% for post in dposts %}
    <url>
        <loc>{{site.url}}/{{post["url"]}}</loc>
        <lastmod>{{ post["time"] | date_to_xmlschema }}</lastmod>
    </url>{% endfor %}
</urlset>